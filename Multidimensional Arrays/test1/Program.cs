using System;
using System.Dynamic;

namespace test1
{
    class Program
    {
        static void Main(string[] args)
        {
            int input = int.Parse(Console.ReadLine());
            int input1 = int.Parse(Console.ReadLine());


            int resul = input1 + input;


            if (resul < 30 )
            {
                Console.WriteLine("Operatora e pederst");
            }

        }
    }
}
